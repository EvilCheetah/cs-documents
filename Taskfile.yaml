version: '3'

vars:
    COMPILER: latexmk
    JOBNAME: Homework 4

tasks:
    cleanup:
        vars:
            FLAGS: -c
        cmds:
            - |
              {{ .COMPILER }} {{ .FLAGS }} -jobname="{{ .JOBNAME }}"

    build:
        dir: ./src/
        deps: [ compile:graphics ]
        vars:
            FLAGS: -pdf
            ENTRYPOINT: main.tex
            
            OUTPUT_DIR: ..
        cmds:
            - |
              {{ .COMPILER }} {{ .FLAGS }}            \
              -jobname="{{ .JOBNAME }}"               \
              -output-directory="./{{ .OUTPUT_DIR }}" \
              {{ .ENTRYPOINT }}
    
    merge:
        vars:
            COMPILER: latexpand
            FLAGS: --keep-comments --verbose
            ENTRYPOINT: main.tex
        cmds:
            - |
              {{ .COMPILER }} {{ .FLAGS }} \
              -output="{{ .JOBNAME }}.tex" \
              {{ .ENTRYPOINT }}

    compile:graphics:
        dir: ./src/
        vars:
            INPUT_DIR: content/figures
            OUTPUT_DIR: content/figures
        deps:
            - task: compile:individual-graphic
              vars: {
                INPUT_DIR:  "{{ .INPUT_DIR }}",
                OUTPUT_DIR: "{{ .OUTPUT_DIR }}",
                FILE:       problem-6-diagram.tikz 
              }
            - task: compile:individual-graphic
              vars: {
                INPUT_DIR:  "{{ .INPUT_DIR }}",
                OUTPUT_DIR: "{{ .OUTPUT_DIR }}",
                FILE:       problem-7-diagram.tikz 
              }
            - task: compile:individual-graphic
              vars: {
                INPUT_DIR:  "{{ .INPUT_DIR }}",
                OUTPUT_DIR: "{{ .OUTPUT_DIR }}",
                FILE:       problem-8-diagram.tikz 
              }
            - task: compile:individual-graphic
              vars: {
                INPUT_DIR:  "{{ .INPUT_DIR }}",
                OUTPUT_DIR: "{{ .OUTPUT_DIR }}",
                FILE:       problem-9-diagram.tikz 
              }
            - task: compile:individual-graphic
              vars: {
                INPUT_DIR:  "{{ .INPUT_DIR }}",
                OUTPUT_DIR: "{{ .OUTPUT_DIR }}",
                FILE:       problem-10-diagram.tikz 
              }

    compile:individual-graphic:
        internal: true
        dir: ./src/
        requires:
            vars: [INPUT_DIR, OUTPUT_DIR, FILE]
        vars:
            FLAGS: -pdf
        cmds:
            - |
              {{ .COMPILER }} {{ .FLAGS }}              \
                -output-directory="./{{ .OUTPUT_DIR }}" \
                {{ .INPUT_DIR }}/{{ .FILE }}
